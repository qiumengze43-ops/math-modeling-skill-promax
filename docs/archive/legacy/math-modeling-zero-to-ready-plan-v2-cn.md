# Archived Legacy Plan

This placeholder marks the old root-level plan as historical and superseded.

Use `AGENTS.md`, `README.md`, `docs/workflow.md`, and `docs/bootstrap.md` for the current project contract. The original unreadable draft remains available in Git history if needed.
